import { Peer as GrpcPeer } from '../../types/rpc_pb';
import { PeerRoleMap } from '../../types/node_pb';
declare type RoleValue = PeerRoleMap[keyof PeerRoleMap];
export declare function keys<O>(o: O): (keyof O)[];
export default class Peer {
    static readonly Role: PeerRoleMap;
    acceptedrole: RoleValue;
    constructor(data: Partial<Peer>);
    static fromGrpc(grpcObject: GrpcPeer): Peer;
    readonly acceptedroleLabel: string;
    toGrpc(): never;
}
export {};
